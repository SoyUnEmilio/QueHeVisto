@model IEnumerable<QueHeVisto.Presentacion.Web.Models.Serie>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Genero)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vista)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EnEmision)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Genero)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vista)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EnEmision)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SerieId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SerieId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SerieId })
        </td>
    </tr>
}

</table>
